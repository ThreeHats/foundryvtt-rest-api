# pnpm settings. As of pnpm 11, `overrides` is read from here, not from the
# `pnpm` field in package.json. These are security floors for transitive deps.
overrides:
  socket.io-parser: '>=4.2.3'
  ws: '>=8.18.0'
  tinymce: '>=7.0.0'
  esbuild: '>=0.25.0'
  parseuri: '>=0.0.6'
  glob: '>=10.5.0'
